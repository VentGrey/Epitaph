#!/bin/sh

artist=$(cmus-remote -Q | awk '/ artist / {for(i=3;i<=NF;++i)printf $i" "}')

if [ -n "$artist" ]; then
    song=$(cmus-remote -Q |\
        awk '/title/ {for(i=3;i<=NF;++i)printf $i" "}')
    printf "%s- %s\n" "$artist" "$song"
else
    echo
fi
